// eslint-config-expo ships no type declarations. Keep this file identical to
// tooling/eslint/eslint-config-expo.d.ts and delete both once the package
// publishes its own types, otherwise this declaration would shadow them.
declare module "eslint-config-expo/flat";
